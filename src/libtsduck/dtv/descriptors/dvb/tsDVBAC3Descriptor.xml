<?xml version="1.0" encoding="UTF-8"?>
<tsduck>
  <_descriptors>

    <!-- DVB version of an AC3 descriptor -->
    <DVB_AC3_descriptor
        component_type="uint8, optional"
        bsid="uint8, optional"
        mainid="uint8, optional"
        asvc="uint8, optional">
      <additional_info>
        Hexadecimal content
      </additional_info>
    </DVB_AC3_descriptor>

    <!-- Previous version, still accepted for compatibility -->
    <AC3_descriptor
        component_type="uint8, optional"
        bsid="uint8, optional"
        mainid="uint8, optional"
        asvc="uint8, optional">
      <additional_info>
        Hexadecimal content
      </additional_info>
    </AC3_descriptor>

  </_descriptors>
</tsduck>
