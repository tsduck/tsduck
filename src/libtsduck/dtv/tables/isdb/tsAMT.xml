<?xml version="1.0" encoding="UTF-8"?>
<tsduck>
  <_tables>

    <!-- ISDB Address Map Table -->
    <AMT version="uint5, default=0" current="bool, default=true">
      <_any in="_metadata"/>
      <!-- One per service -->
      <service service_id="uint16, required"
               src="ip-address/prefix, required"
               dst="ip-address/prefix, required">
        <private_data>
          Hexadecimal content
        </private_data>
      </service>
    </AMT>

  </_tables>
</tsduck>
